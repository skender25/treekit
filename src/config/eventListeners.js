import styled from "styled-components";

export const handleAddToCart = configurationState => {};

export const handleInputSelect = () => {
    document.getElementById("loader-container").style.display = "none";
};
